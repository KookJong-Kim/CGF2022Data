1. 참석률 분석
목적: 교육 대상 인원 대비 실제 참석한 인원의 비율을 계산하고 분석
전처리:
참석률 = (참석인원수 / 대상인원수) * 100 계산
참석률이 0% 미만이거나 100% 초과인 이상치를 제거
출력: 참석률의 기본 통계량 (summary(data$참석률))
시각화: 히스토그램(ggplot2)을 사용하여 참석률 분포를 확인
2. 강사별 평균 참석률 분석
목적: 강사별 평균 참석률을 비교하여, 교육을 진행하는 강사마다 참석률의 차이를 확인
전처리:
강사별로 참석률 평균(mean()) 계산
높은 순서로 정렬(arrange(desc(평균참석률)))
출력: 강사별 평균 참석률을 포함하는 데이터
시각화: 막대그래프(ggplot2)로 강사별 평균 참석률을 표현
3. 삭제된 데이터 비율 분석
목적: 삭제된 데이터(삭제여부 컬럼 값이 "Y")의 비율을 확인하여 데이터 정합성을 검토
전처리:
삭제여부 컬럼을 기준으로 그룹화하고 비율(n() / nrow(data) * 100) 계산
출력: 삭제된 데이터와 유지된 데이터의 비율
시각화: 원형 그래프(ggplot2의 coord_polar)로 삭제된 데이터의 비율을 표현
4. 강사별 교육 참가율 비교
목적: 각 강사의 교육 참가율을 비교하여, 교육 대상 대비 실제 참석한 인원 비율을 분석
전처리:
총 대상 인원수, 총 참석 인원수를 계산 (sum(대상인원수), sum(참석인원수))
교육 참가율 = (총 참석인원 / 총 대상인원) * 100 계산
높은 순서로 정렬 (arrange(desc(교육참가율)))
출력: 강사별 교육 참가율 데이터
시각화: 막대그래프(ggplot2)로 강사별 교육 참가율을 표현
5. 특정 직원의 데이터 처리 빈도 분석
목적: 직원별 데이터 처리 빈도를 비교하여 특정 직원이 데이터를 처리한 횟수를 분석
전처리:
처리직원번호별로 그룹화 후 처리 횟수(n()) 계산
높은 순서로 정렬 (arrange(desc(처리횟수)))
출력: 특정 직원이 처리한 데이터 개수
시각화: 막대그래프(ggplot2)로 특정 직원의 데이터 처리 빈도를 표현
요약
이 코드는 참석률 분석, 강사별 분석, 삭제된 데이터 분석, 참가율 비교, 특정 직원 처리 빈도 분석을 포함합니다.
각 분석에서는 데이터 전처리, 통계 요약, 시각화(히스토그램, 막대그래프, 원형 그래프 등)를 수행합니다.
